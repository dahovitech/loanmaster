{% extends '@emails/layout.html.twig' %}

{% block subject %}
    {{ 'email.subject.loanOrderPay'|trans({}, 'messages', user.locale) }}
{% endblock %}

{% block body %}
    <p>{{ 'email.body.loanOrderPay.greeting'|trans({'%user%': user.firstname}, 'messages', user.locale) }}</p>
    
    <p>{{ 'email.body.loanOrderPay.message'|trans({'%loanNumber%': loan.loanNumber, '%url%': url}, 'messages', user.locale) |raw }}</p>
    <p>{{ 'email.body.loanOrderPay.contact'|trans({'%email%': setting('email')}, 'messages', user.locale) |raw  }}</p>
{% endblock %}
