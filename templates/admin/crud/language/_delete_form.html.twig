<form method="post" action="{{ path('app_crud_delete', {'id': entity.id,entityName: 'language'}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entity.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
