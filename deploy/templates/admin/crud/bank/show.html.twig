{% extends '@admin/base.html.twig' %}

{% block title %}Bank{% endblock %}

{% block body %}
    <h1>Bank</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom de la banque</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ entity.logo }}</td>
            </tr>
            <tr>
                <th>Lien de la banque</th>
                <td>{{ entity.url }}</td>
            </tr>
            <tr>
                <th>Nom et prénoms du notaire</th>
                <td>{{ entity.natary }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-block btn-outline-primary" href="{{ path('app_crud_index',{entityName: 'bank'}) }}">back to list</a>

    <a class="btn btn-block btn-primary" href="{{ path('app_crud_update', {'id': entity.id,entityName: 'bank'}) }}">edit</a>

    {{ include('@admin/crud/bank/_delete_form.html.twig') }}
{% endblock %}
