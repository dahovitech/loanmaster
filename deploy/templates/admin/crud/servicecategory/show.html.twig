{% extends '@admin/base.html.twig' %}

{% block title %}{{ 'serviceCategory.show'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'serviceCategory.show'|trans }}</h1>

    <!--breadcrumb-->
    <div class="page-breadcrumb d-none d-sm-flex align-items-center mb-3">
        <div class="breadcrumb-title pe-3">{{ 'serviceCategory.show'|trans }}</div>
        <div class="ps-3">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-0 p-0">
                    <li class="breadcrumb-item">
                        <a href="{{ path('app_admin_dashboard') }}">
                            <i class="bx bx-home-alt"></i>
                        </a>
                    </li>
                    <li class="breadcrumb-item">
                        <a href="{{ path('app_crud_index',{entityName: entityName}) }}">
                            <i class="bx bx-list-alt"></i> {{ 'serviceCategory.list'|trans }}
                        </a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">{{ 'serviceCategory.show'|trans }}</li>
                </ol>
            </nav>
        </div>
    </div>
    <!--end breadcrumb-->
    <div class="row">
        <div class="col-md-10">
            <div class="card">
                <div class="card-body">
                    <p><strong>{{ 'serviceCategory.name'|trans }}:</strong> {{ entity.name }}</p>
                    <p><strong>{{ 'tab.status'|trans }}:</strong> {{ entity.isEnabled ? 'status.enabled'|trans : 'status.disabled'|trans }}</p>
                </div>

                <a class="btn btn-block btn-outline-primary" href="{{ path('app_crud_index',{entityName: entityName}) }}">{{ 'back_to_list'|trans }}</a>
            </div>
        </div>
    </div>
{% endblock %}
